<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Borderless black: the emulator surface fills the screen and the touch
         overlay draws on top of it. Black window background so the letterbox
         bars around the 4:3 DOS image read as part of the game. -->
    <style name="Theme.Keen456" parent="@android:style/Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
