<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Keen runs a 320x200 DOS game; no phone feature is required. -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />

    <!-- Save sync (⚙ ▸ Sync) talks to the user-configured sync server. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:hardwareAccelerated="true"
        android:extractNativeLibs="true"
        android:theme="@style/Theme.Keen456">

        <!-- Episode picker: choose Keen 4/5/6, import Keen 5/6 game files. -->
        <activity
            android:name=".PickerActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The emulator. Runs in its OWN process: SDL_main can only run once
             per process (static state everywhere), so giving each play session
             a fresh :game process is what lets the player return to the picker
             and start another episode without relaunching the whole app. -->
        <activity
            android:name=".KeenActivity"
            android:exported="false"
            android:process=":game"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:launchMode="singleInstance"
            android:preferMinimalPostProcessing="true"
            android:resizeableActivity="false"
            android:alwaysRetainTaskState="true"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation">
        </activity>
    </application>

</manifest>
